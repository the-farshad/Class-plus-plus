[
  {
    "session_tag": "prog01",
    "prompt": "**Prog 01 — Headers and I/O.**\n\nWhich C++ standard header is required for `std::cin` and `std::cout`?",
    "type": "poll",
    "options": ["`<iostream>`", "`<stdio.h>`", "`<cstdlib>`", "`<string>`"]
  },
  {
    "session_tag": "prog01",
    "prompt": "**Prog 01 — `cin` failure modes.**\n\nGiven:\n\n```cpp\nint a, b, c, d;\nstd::cin >> a >> b >> c >> d;\n```\n\nThe user types `5 10 abc 20`. After the read, what's true about `c`?",
    "type": "poll",
    "options": [
      "`c` is unchanged AND the stream goes into a fail state (subsequent reads do nothing until cleared)",
      "`c == 0` (the failed extraction default-zeroes the int)",
      "`c == 'a'` (the first character of the bad token is stored as an `int`)",
      "`c == 20` (the bad token is skipped and the next int is read)"
    ]
  },
  {
    "session_tag": "prog01",
    "prompt": "**Prog 01 — Pre-coding step.**\n\nSubmit a **photo of your hand-written pseudocode** for Prog 01 *before* you look at the solution. Include your name and the date on the page.\n\n*Typed answers and AI-generated images receive zero credit — the assignment is about your handwriting and your thinking.*",
    "type": "submission"
  },

  {
    "session_tag": "prog02",
    "prompt": "**Prog 02 — Ordering the algorithm.**\n\nThe pseudocode for Prog 02 (compute the running product over an interval) does these things, in some order. Drag them into the **correct order**.",
    "type": "ordering",
    "options": [
      "Prompt user for two integers",
      "Read the two values",
      "If value1 > value2, swap them",
      "Set accumulator = value1",
      "While value1 < value2: increment value1, then multiply accumulator by value1",
      "Output the accumulator"
    ]
  },
  {
    "session_tag": "prog02",
    "prompt": "**Prog 02 — Why swap?**\n\nThe pseudocode interchanges `value1` and `value2` when `value1 > value2`. Why is that necessary?",
    "type": "poll",
    "options": [
      "So the while loop is guaranteed to execute *and* the smaller value drives the loop counter to termination",
      "Because C++ requires the left operand of `<` to be smaller",
      "To avoid integer overflow when multiplying",
      "Stylistic only — the code works either way"
    ]
  },
  {
    "session_tag": "prog02",
    "prompt": "**Prog 02 — Confidence check.**\n\nOn a scale of 1–10, how confident do you feel with `while` loops **after** finishing Prog 02?",
    "type": "rating"
  },

  {
    "session_tag": "prog03",
    "prompt": "**Prog 03 — Loop selection.**\n\nWhich loop is **guaranteed** to execute its body at least once, even when the condition is false on entry?",
    "type": "poll",
    "options": ["`for`", "`while`", "`do … while`", "range-based `for`"]
  },
  {
    "session_tag": "prog03",
    "prompt": "**Prog 03 — Trace this.**\n\nCount integers *strictly between* two values (endpoints excluded). The user enters `4` and `9`.\n\nThe pseudocode does:\n\n```text\ncounter = 0\nloopctl = smaller + 1     // = 5\nwhile loopctl < larger:    // larger = 9\n    counter = counter + 1\n    loopctl  = loopctl + 1\n```\n\nWhat is the final value of `counter`?",
    "type": "poll",
    "options": ["3", "4", "5", "6"]
  },
  {
    "session_tag": "prog03",
    "prompt": "**Prog 03 — One word.**\n\nWhich C++ keyword exits a loop *immediately*?",
    "type": "word_cloud"
  },

  {
    "session_tag": "prog04",
    "prompt": "**Prog 04 — Designing the validator.**\n\nThe `prompt` function loops until the input is non-negative. What is the **simplest** return type for that function?",
    "type": "poll",
    "options": ["`int` (return the validated value)", "`bool` (true on success)", "`void` (mutate a reference parameter)", "`std::string` (return the user's input verbatim)"]
  },
  {
    "session_tag": "prog04",
    "prompt": "**Prog 04 — Recursion sanity.**\n\nFor `n = 5`, what does a correct `factorial(n)` return?",
    "type": "poll",
    "options": ["25", "120", "720", "Stack overflow"]
  },
  {
    "session_tag": "prog04",
    "prompt": "**Prog 04 — Trace the stack.**\n\nOn paper, **by hand**, draw the call stack for `factorial(4)` using a recursive implementation. Show every frame and the return value bubbling up.\n\nPhotograph or scan and submit. Typed or AI-generated diagrams will get **zero** — the conventions you use *in class* are what's being graded.",
    "type": "submission"
  },

  {
    "session_tag": "prog05",
    "prompt": "**Prog 05 — Series term sign.**\n\nThe Madhava–Leibniz series for `π/4` is:\n\n```text\n1 − 1/3 + 1/5 − 1/7 + …\n```\n\nWhich loop pattern best fits computing terms with alternating signs?",
    "type": "poll",
    "options": [
      "`for`-loop with `sign = -sign` each iteration",
      "`while`-loop and recompute `pow(-1, k)` every iteration",
      "`do-while` with a `switch` on `k % 4`",
      "Use `std::accumulate` over a vector of pre-computed signs"
    ]
  },
  {
    "session_tag": "prog05",
    "prompt": "**Prog 05 — Convergence intuition.**\n\nA student's first run with `n = 100` prints `3.13159…` and they think it's accurate. What's actually true about the Madhava–Leibniz series?",
    "type": "poll",
    "options": [
      "It converges quickly — `n = 100` is already accurate to 6 digits",
      "It converges slowly — you need on the order of **10⁶** terms for 6-digit accuracy",
      "It diverges; the partial sums oscillate forever",
      "Accuracy depends on whether `n` is even or odd"
    ]
  },
  {
    "session_tag": "prog05",
    "prompt": "**Prog 05 — Function decomposition.**\n\nWhich split is the cleanest for this program?",
    "type": "poll_pie",
    "options": [
      "`promptForN()` · `computePi(n)` · `displayResult(pi)`",
      "Everything in `main()`",
      "One big function that reads, computes, and prints",
      "`promptForN()` · `computePi(n, &output)` — pass result by pointer"
    ]
  },

  {
    "session_tag": "prog06",
    "prompt": "**Prog 06 — Validate the constraint.**\n\nWall thickness `t` must satisfy:\n\n```text\nt ≤ ½ · min(d, h)\n```\n\nUser enters `d = 10`, `h = 6`, `t = 4`. Is this valid?",
    "type": "poll",
    "options": [
      "**Invalid** — ½ · min(10, 6) = 3, and 4 > 3",
      "Valid — `t` just has to be positive",
      "Valid — 4 ≤ ½ · max(10, 6) = 5",
      "Invalid — `t` must be strictly less than `d`"
    ]
  },
  {
    "session_tag": "prog06",
    "prompt": "**Prog 06 — Re-prompting behavior.**\n\nIn your validation loop, the user enters a negative diameter **three times** in a row, then a valid one.\n\nRoughly how many times does your `prompt` function print the prompt string?",
    "type": "poll",
    "options": ["1", "3", "4", "5"]
  },
  {
    "session_tag": "prog06",
    "prompt": "**Prog 06 — Sketch the geometry.**\n\nPhotograph **your hand-sketched** cross-section of the cylinder showing:\n\n- height `h`\n- diameter `d`\n- wall thickness `t`\n- the constraint `t ≤ ½·min(d, h)` *labeled on the sketch*\n\nSubmit the image. No CAD, no AI — pencil-on-paper only.",
    "type": "submission"
  }
]
